# Fix RTL Silverbulle (Firefox Extension)

A Firefox extension that automatically fixes Silverbulle RTL (Right-To-Left) rendering issues for Arabic text in editors such as SilverBullet, CodeMirror, Markdown viewers, and general web content.

The extension allows you to:

- ✔ Apply RTL fix **manually** by clicking the extension button  
- ✔ Apply RTL fix **automatically** on specific websites you choose  
- ✔ Detect English-only lines and set them LTR automatically  
- ✔ Apply Arabic-friendly fonts (Noto Naskh Arabic / Readex Pro)  
- ✔ Fully customize which websites receive the automatic RTL fix  

---

## ✨ Features

### 🔹 Manual Mode
Click the extension icon from the Firefox toolbar to immediately apply the RTL fix to the current webpage.

### 🔹 Automatic Mode
You can enable automatic RTL fixing on any websites of your choice through the built-in **Options Page**.

### 🔹 Smart Line Detection
The extension automatically:
- Detects whether a line contains Arabic or English  
- Sets Arabic lines as RTL  
- Sets English lines as LTR  
- Supports auto-update via MutationObserver  

### 🔹 Arabic-Optimized Styling
The extension injects a full RTL CSS layer that:
- Forces RTL direction  
- Aligns text to the right  
- Applies Arabic-friendly fonts **Noto Naskh Arabic**
- Keeps code blocks, tables, and inline code LTR  

---

## 📥 Installation (Temporary)

To install the extension manually in Firefox:

1. Open Firefox and visit:  
